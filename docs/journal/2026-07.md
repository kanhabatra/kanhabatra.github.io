### 2026-07-13 — Documentation scaffold + current-state snapshot

- **Update:** Created initial `docs/` documentation scaffold for the repository: `docs/blueprint.md`, `docs/journal/index.md`, and this journal file. No existing repository files were modified, moved, or deleted; no git commands were run.
- **Context:** Repository is an al-folio Jekyll academic site for Kanha Batra, deployed to GitHub Pages (`https://kanhabatra.github.io`). Documentation was assembled by reading `AGENTS.md` (full), the top of `README.md`, the top of `_config.yml`, and the `.github/workflows/` directory (notably `deploy.yml` and `render-cv.yml`), plus a directory listing of the repo root.
- **Outcome:** Blueprint captures repository layout (`_pages`, `_projects`, `_news`, `_data`, `_bibliography`, `_includes`, `_layouts`, `_sass`, `assets`, `.github`), confirms the deploy path is GitHub Actions (`deploy.yml`) building with `bundle exec jekyll build` and publishing via `actions/deploy-pages` to GitHub Pages, and confirms Docker (`Dockerfile`, `docker-compose.yml`) is local-dev-only per `AGENTS.md`.
- **Open questions:** `_config.yml`'s `exclude:` list (lines 172-191) does not currently list `docs/`. Since al-folio's Jekyll build does not exclude non-underscore top-level directories by default, a top-level `docs/` folder of markdown may be picked up and processed by Jekyll at build time. This was flagged in the task report but `_config.yml` was intentionally left unmodified per task constraints.
- **Follow-ups:** Recommend the user (or a future task) add `docs/` to the `exclude:` list in `_config.yml` to prevent the documentation from being built/published as part of the site. Also confirm whether `docs/` should be listed in `.prettierignore` / other tooling ignore files if prettier or link-checking workflows (`prettier.yml`, `broken-links-site.yml`) would otherwise process it.
- **Notes:** No content, structural, or configuration changes were made to the site itself in this pass — this was a documentation-only scaffold.
